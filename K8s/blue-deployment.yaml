apiVersion: apps/v1
kind: Deployment

metadata:
  name: demo-blue

spec:
  replicas: 2

  selector:
    matchLabels:
      app: demo
      version: blue

  template:
    metadata:
      labels:
        app: demo
        version: blue

    spec:
      containers:
      - name: demo

        image: ACCOUNT_ID.dkr.ecr.REGION.amazonaws.com/demo-app:1

        ports:
        - containerPort: 80